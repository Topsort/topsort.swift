version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      # upload-artifact and download-artifact share a backend and must move together.
      artifact-actions:
        patterns:
          - actions/upload-artifact
          - actions/download-artifact
